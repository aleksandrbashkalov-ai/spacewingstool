# spacewingstool
AI-native macOS workspace manager with local on-device intelligence. Automatically creates and switches spaces based on your context. Tracks reading, writing, email, media, meetings — all off by default, fully local. Built with SwiftUI, zero telemetry, no dependencies beyond GRDB.
