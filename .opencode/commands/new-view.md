---
description: Create a new SwiftUI view file
---

Create a new SwiftUI view file in Sources/UI/ with proper structure and Preview macro.

```bash
touch Sources/UI/$ARGUMENTS.swift
```

Then generate the view file with:
- import SwiftUI
- struct with Body view
- Preview macro
- Follow existing patterns from MenuBarView.swift or SpaceListView.swift
