---
description: Build the project and run all tests
---

Build the Spacewingstool project and run the test suite to verify everything compiles and passes.

```bash
swift build && swift test
```

If tests fail, analyze the failures and fix them before reporting back.
